import { TouchableOpacity, View } from "react-native"
import { SafeArea } from "./StyleComponent"

export default MettingBefore = ({route, navigation}) => {
    <SafeArea>
        <View style={{flex: 1}}>
            <TouchableOpacity>
                <Text> 회의 세부 내용</Text>
            </TouchableOpacity>
        </View>
    </SafeArea>
}